# Backend Tests

Backend unit and API tests live here. Test business behavior and failure cases without
coupling tests to implementation details.

Run:

```bash
docker compose exec backend pytest
```
